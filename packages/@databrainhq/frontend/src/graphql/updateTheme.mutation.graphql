mutation UpdateTheme($id: uuid = "", $_set: themes_set_input = {}) {
  update_themes_by_pk(pk_columns: { id: $id }, _set: $_set) {
    cardCustomization
    chartCustomization
    cardDescription
    cardTitle
    chart
    companyId
    dashboard
    general
    id
    workspaceId
  }
}
